This is starter code for a react API. The only functionality it possesses is auth0 login, logout, signup and profile accessibility.

It is completely stripped down. Note there is no nav bar, routes, etc. 